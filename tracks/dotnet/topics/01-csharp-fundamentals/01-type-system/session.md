# Type System — Сессии

## Сессия 1 — 2026-03-05

### Калибровка
- Q1 (базовый): Какие типы знаешь, две группы? → ✅ «ссылочные типы и типы значений»
- Q2 (средний): int a=5; int b=a; b=10; a=? → ✅ «5»
- Q3 (продвинутый): struct vs class? → ✅ «struct — value type, не наследуется, сравнение по значениям, аналогия с монетами»
- Результат: solid Junior+, основы знает, углубляем

---

### Блок 1: CTS — Common Type System

**Claude:** Аналогия с библиотекой — Object как универсальная карточка. Иерархия: Object → ValueType → конкретные типы. Unified type system. Парадокс ValueType (сам reference type, наследники — value types). String — reference type с поведением value type.

**Код:** typeof(int).BaseType → ValueType → Object, IsValueType проверки.

### Проверка Блока 1
**Q1:** От чего наследуется bool? А тот тип?
**Адиль:** «от valueType» → ✅ (частично — не назвал вторую часть цепочки Object)

**Q2:** Почему ValueType — class, а не struct?
**Адиль:** «чтоб были сигнатуры базовых операций как equals, и чтоб можно было преобразовать тип» → ✅ (хорошее рассуждение, упустил циркулярную зависимость)

**Фидбек:** понятно ✅

#### ↳ Углубление: Boxing performance (тип: любопытство)
**Адиль:** «почему boxing дорого по производительности?»
**Claude:** Объяснил: heap allocation (~20 байт вместо 4), копирование, нагрузка на GC. Пример ArrayList vs List<T>. Generics как решение проблемы boxing.

---

**Итог сессии:** Блок 1 пройден. Остановились перед Блоком 2.
