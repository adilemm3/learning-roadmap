{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "command": "cd ~/Documents/myProjects/learning-roadmap && git add -A && git diff --cached --quiet || git commit -m \"auto: update $(date +%Y-%m-%d_%H:%M)\""
      }
    ]
  }
}
